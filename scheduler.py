# scheduler.py

def start_scheduler_thread():
    """
    Temporary stub so the app boots.
    Replace with the real async scheduler later.
    """
    print("[scheduler] start_scheduler_thread() called — stub running")
